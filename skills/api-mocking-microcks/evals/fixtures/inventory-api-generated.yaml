# Code generated by inventory-service annotation processor. DO NOT EDIT.
# Regenerated on every release build; manual changes will be overwritten.
openapi: 3.0.3
info:
  title: Inventory API
  version: 2.3.0
  description: Read and reserve warehouse inventory.
paths:
  /items/{sku}:
    get:
      operationId: getItemBySku
      parameters:
        - name: sku
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Item found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Item"
        "404":
          description: No item with this SKU
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /reservations:
    post:
      operationId: createReservation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReservationRequest"
      responses:
        "201":
          description: Reservation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reservation"
components:
  schemas:
    Item:
      type: object
      required: [sku, name, stock]
      properties:
        sku:
          type: string
        name:
          type: string
        stock:
          type: integer
    ReservationRequest:
      type: object
      required: [sku, quantity]
      properties:
        sku:
          type: string
        quantity:
          type: integer
    Reservation:
      type: object
      required: [reservationId, sku, quantity, status]
      properties:
        reservationId:
          type: string
        sku:
          type: string
        quantity:
          type: integer
        status:
          type: string
    Error:
      type: object
      required: [message]
      properties:
        message:
          type: string
