# payments-gateway

Card authorisation and capture gateway. Go 1.23.

```bash
make help     # every target
make run      # local server on :8080
make ci       # lint + tests
```

Agent guidance lives in [AGENTS.md](AGENTS.md). Releasing is described in
docs/releasing.md.
