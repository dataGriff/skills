<!-- What changed and why. -->

## Eval evidence

<!-- Required when a PR touches a skill. Paste the table from
skills/<name>/evals/latest-results.md — refreshed in this branch via
`task eval:skills NAME=<name>` — or state why no re-run was needed
(e.g. typo-only change). CI also posts this as a sticky PR comment
(`task pr:eval-summary` shows it locally). -->
